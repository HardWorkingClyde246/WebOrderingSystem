<?xml version="1.0"?>
<configuration>
	<authorization>
		<!-- Allow access only to users in the Role 1 -->
		<allow roles="1" />

		<!-- Deny access to all other users -->
		<deny users="*" />
		
	</authorization>
	
	
  <system.web>

  </system.web>
</configuration>
